# wrpl

Parser and extensible deserializer for Dagor Engine's replay format.

## build

```bash
git clone --recursive https://github.com/moleium/wrpl
cd wrpl
mkdir build && cd build
cmake -G Ninja ..
ninja
```

## run

```bash
./wrpl <path_to_replay>
```
